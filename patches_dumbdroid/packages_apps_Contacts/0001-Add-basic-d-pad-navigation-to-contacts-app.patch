From eb35235e687539f9312e98b1b0d8573369ec89c9 Mon Sep 17 00:00:00 2001
From: Michal Brzozowski <miki@extremic.com>
Date: Mon, 12 Jan 2026 10:09:03 +0100
Subject: [PATCH 1/2] Add basic d-pad navigation to contacts app

Change-Id: Ibd34911bb376cb95f298c4de2719a7b5f2446452
---
 .../contacts/activities/ActionBarAdapter.java |  14 +-
 .../contacts/activities/PeopleActivity.java   | 187 ++++++++++++++++++
 .../DefaultContactBrowseListFragment.java     |  29 +++
 3 files changed, 229 insertions(+), 1 deletion(-)

diff --git a/src/com/android/contacts/activities/ActionBarAdapter.java b/src/com/android/contacts/activities/ActionBarAdapter.java
index e6f29a4ab..2878e020a 100644
--- a/src/com/android/contacts/activities/ActionBarAdapter.java
+++ b/src/com/android/contacts/activities/ActionBarAdapter.java
@@ -190,6 +190,7 @@ public class ActionBarAdapter implements OnCloseListener {
                         }
                     }
                 });
+        updateDpadNavigation();
     }
 
     public void initialize(Bundle savedState, ContactsRequest request) {
@@ -274,9 +275,13 @@ public class ActionBarAdapter implements OnCloseListener {
                 mSearchView.setEnabled(false);
             }
             setQueryString(null);
+            updateDpadNavigation();
         } else if (flag) {
             // Everything is already set up. Still make sure the keyboard is up
-            if (mSearchView != null) setFocusOnSearchView();
+            if (mSearchView != null) {
+                setFocusOnSearchView();
+                updateDpadNavigation();
+            }
         }
     }
 
@@ -284,6 +289,7 @@ public class ActionBarAdapter implements OnCloseListener {
         if (mSelectionMode != flag) {
             mSelectionMode = flag;
             update(false /* skipAnimation */);
+            updateDpadNavigation();
         }
     }
 
@@ -436,6 +442,12 @@ public class ActionBarAdapter implements OnCloseListener {
         }
     }
 
+    private void updateDpadNavigation() {
+        if (mActivity instanceof PeopleActivity) {
+            ((PeopleActivity) mActivity).updateDpadNavigation();
+        }
+    }
+
     /**
      * Find overflow menu ImageView by its content description and update its color.
      */
diff --git a/src/com/android/contacts/activities/PeopleActivity.java b/src/com/android/contacts/activities/PeopleActivity.java
index 62dc48ef4..e0e90aac8 100644
--- a/src/com/android/contacts/activities/PeopleActivity.java
+++ b/src/com/android/contacts/activities/PeopleActivity.java
@@ -30,6 +30,7 @@ import android.content.IntentFilter;
 import android.content.SyncStatusObserver;
 import android.content.res.Configuration;
 import android.graphics.Color;
+import android.graphics.drawable.Drawable;
 import android.net.Uri;
 import android.os.Bundle;
 import android.os.Handler;
@@ -55,7 +56,9 @@ import android.view.View;
 import android.view.ViewGroup;
 import android.view.accessibility.AccessibilityEvent;
 import android.view.accessibility.AccessibilityManager;
+import android.widget.AdapterView;
 import android.widget.ImageButton;
+import android.widget.ListView;
 import android.widget.Toast;
 
 import com.android.contacts.AppCompatContactsActivity;
@@ -71,6 +74,7 @@ import com.android.contacts.group.GroupMembersFragment;
 import com.android.contacts.group.GroupNameEditDialogFragment;
 import com.android.contacts.group.GroupUtil;
 import com.android.contacts.list.AccountFilterActivity;
+import com.android.contacts.list.ContactListAdapter;
 import com.android.contacts.list.ContactListFilter;
 import com.android.contacts.list.ContactListFilterController;
 import com.android.contacts.list.ContactListFilterController.ContactListFilterListener;
@@ -187,6 +191,7 @@ public class PeopleActivity extends AppCompatContactsActivity implements
     private DrawerFragment mDrawerFragment;
     private ContactsActionBarDrawerToggle mToggle;
     private Toolbar mToolbar;
+    private boolean mRequestInitialToolbarFocus;
 
     // The account the new group will be created under.
     private AccountWithDataSet mNewGroupAccount;
@@ -404,6 +409,7 @@ public class PeopleActivity extends AppCompatContactsActivity implements
         mProviderStatusWatcher.addListener(mProviderStatusListener);
 
         mIsRecreatedInstance = (savedState != null);
+        mRequestInitialToolbarFocus = !mIsRecreatedInstance;
 
         if (mIsRecreatedInstance) {
             mGroupUri = savedState.getParcelable(KEY_GROUP_URI);
@@ -1103,6 +1109,187 @@ public class PeopleActivity extends AppCompatContactsActivity implements
         return mToolbar;
     }
 
+    public void updateDpadNavigation() {
+        if (mToolbar == null) {
+            return;
+        }
+        mToolbar.post(new Runnable() {
+            @Override
+            public void run() {
+                updateDpadNavigationInternal();
+            }
+        });
+    }
+
+    private void updateDpadNavigationInternal() {
+        View navButton = findToolbarNavigationButton();
+        View searchButton = mToolbar.findViewById(R.id.menu_search);
+        View searchView = mToolbar.findViewById(R.id.search_view);
+        if (navButton != null && navButton.getVisibility() != View.VISIBLE) {
+            navButton = null;
+        }
+        if (searchButton != null && searchButton.getVisibility() != View.VISIBLE) {
+            searchButton = null;
+        }
+        if (searchView != null && searchView.getVisibility() != View.VISIBLE) {
+            searchView = null;
+        }
+
+        ListView listView = null;
+        if (mContactsListFragment != null) {
+            listView = mContactsListFragment.getListView();
+        }
+
+        View.OnKeyListener focusDownListener = null;
+        if (listView != null) {
+            final ContactListAdapter selectionAdapter = mContactsListFragment != null
+                    ? mContactsListFragment.getAdapter() : null;
+            final ListView focusListView = listView;
+            focusDownListener = new View.OnKeyListener() {
+                @Override
+                public boolean onKey(View v, int keyCode, KeyEvent event) {
+                    if (keyCode != KeyEvent.KEYCODE_DPAD_DOWN
+                            || event.getAction() != KeyEvent.ACTION_DOWN) {
+                        return false;
+                    }
+                    if (focusListView.getVisibility() != View.VISIBLE) {
+                        return false;
+                    }
+                    focusListView.requestFocus();
+                    if (focusListView.getSelectedItemPosition() == AdapterView.INVALID_POSITION
+                            && focusListView.getCount() > 0) {
+                        int selectedPosition = AdapterView.INVALID_POSITION;
+                        if (selectionAdapter != null) {
+                            selectedPosition = selectionAdapter.getSelectedContactPosition();
+                        }
+                        if (selectedPosition != AdapterView.INVALID_POSITION) {
+                            focusListView.setSelection(selectedPosition
+                                    + focusListView.getHeaderViewsCount());
+                        } else {
+                            focusListView.setSelection(focusListView.getFirstVisiblePosition());
+                        }
+                    }
+                    return true;
+                }
+            };
+        }
+
+        View.OnKeyListener listUpListener = null;
+        if (listView != null && searchView != null) {
+            final ListView focusListView = listView;
+            final View focusSearchView = searchView;
+            listUpListener = new View.OnKeyListener() {
+                @Override
+                public boolean onKey(View v, int keyCode, KeyEvent event) {
+                    if (keyCode != KeyEvent.KEYCODE_DPAD_UP
+                            || event.getAction() != KeyEvent.ACTION_DOWN) {
+                        return false;
+                    }
+                    if (focusSearchView.getVisibility() != View.VISIBLE) {
+                        return false;
+                    }
+                    final int selectedPosition = focusListView.getSelectedItemPosition();
+                    final int headerCount = focusListView.getHeaderViewsCount();
+                    if (selectedPosition == AdapterView.INVALID_POSITION
+                            || selectedPosition <= headerCount) {
+                        focusSearchView.requestFocus();
+                        return true;
+                    }
+                    return false;
+                }
+            };
+        }
+
+        if (navButton != null) {
+            navButton.setFocusable(true);
+            navButton.setFocusableInTouchMode(true);
+            if (listView != null && listView.getId() != View.NO_ID) {
+                navButton.setNextFocusDownId(listView.getId());
+            }
+            if (focusDownListener != null) {
+                navButton.setOnKeyListener(focusDownListener);
+            }
+        }
+        if (searchButton != null) {
+            searchButton.setFocusable(true);
+            searchButton.setFocusableInTouchMode(true);
+            if (listView != null && listView.getId() != View.NO_ID) {
+                searchButton.setNextFocusDownId(listView.getId());
+            }
+            if (focusDownListener != null) {
+                searchButton.setOnKeyListener(focusDownListener);
+            }
+        }
+        if (searchView != null) {
+            searchView.setFocusable(true);
+            searchView.setFocusableInTouchMode(true);
+            if (listView != null && listView.getId() != View.NO_ID) {
+                searchView.setNextFocusDownId(listView.getId());
+            }
+            if (focusDownListener != null) {
+                searchView.setOnKeyListener(focusDownListener);
+            }
+        }
+        if (navButton != null && searchButton != null) {
+            navButton.setNextFocusRightId(searchButton.getId());
+            searchButton.setNextFocusLeftId(navButton.getId());
+        }
+
+        if (listView != null && listView.getId() != View.NO_ID) {
+            if (searchView != null) {
+                listView.setNextFocusUpId(searchView.getId());
+            } else if (navButton != null) {
+                listView.setNextFocusUpId(navButton.getId());
+            } else if (searchButton != null) {
+                listView.setNextFocusUpId(searchButton.getId());
+            }
+            listView.setOnKeyListener(listUpListener);
+        }
+
+        if (mRequestInitialToolbarFocus) {
+            View initialFocus = navButton != null ? navButton : searchButton;
+            if (initialFocus != null) {
+                initialFocus.requestFocus();
+                mRequestInitialToolbarFocus = false;
+            }
+        }
+    }
+
+    private View findToolbarNavigationButton() {
+        if (mToolbar == null) {
+            return null;
+        }
+        final Drawable navIcon = mToolbar.getNavigationIcon();
+        final CharSequence navDescription = mToolbar.getNavigationContentDescription();
+        final int childCount = mToolbar.getChildCount();
+        ImageButton firstButton = null;
+        for (int i = 0; i < childCount; i++) {
+            View child = mToolbar.getChildAt(i);
+            if (!(child instanceof ImageButton)) {
+                continue;
+            }
+            ImageButton button = (ImageButton) child;
+            if (firstButton == null) {
+                firstButton = button;
+            }
+            if (navDescription != null && navDescription.equals(button.getContentDescription())) {
+                return button;
+            }
+            if (navIcon != null && button.getDrawable() != null) {
+                Drawable childDrawable = button.getDrawable();
+                if (childDrawable == navIcon) {
+                    return button;
+                }
+                if (childDrawable.getConstantState() != null
+                        && navIcon.getConstantState() != null
+                        && childDrawable.getConstantState().equals(navIcon.getConstantState())) {
+                    return button;
+                }
+            }
+        }
+        return firstButton;
+    }
+
     @Override
     protected void onPostCreate(Bundle savedInstanceState) {
         super.onPostCreate(savedInstanceState);
diff --git a/src/com/android/contacts/list/DefaultContactBrowseListFragment.java b/src/com/android/contacts/list/DefaultContactBrowseListFragment.java
index fd6fc8cb8..7f9284288 100644
--- a/src/com/android/contacts/list/DefaultContactBrowseListFragment.java
+++ b/src/com/android/contacts/list/DefaultContactBrowseListFragment.java
@@ -54,6 +54,7 @@ import android.widget.Button;
 import android.widget.FrameLayout;
 import android.widget.ImageView;
 import android.widget.LinearLayout.LayoutParams;
+import android.widget.ListView;
 import android.widget.TextView;
 import android.widget.Toast;
 
@@ -154,6 +155,7 @@ public class DefaultContactBrowseListFragment extends ContactBrowseListFragment
     private boolean mDisableOptionItemSelected;
 
     private boolean mSearchResultClicked;
+    private boolean mRestoreSelectionOnResume;
 
     private ActionBarAdapter mActionBarAdapter;
     private PeopleActivity mActivity;
@@ -289,6 +291,7 @@ public class DefaultContactBrowseListFragment extends ContactBrowseListFragment
         if (mActionBarAdapter != null) {
             mActionBarAdapter.updateOverflowButtonColor();
         }
+        maybeRestoreSelection();
     }
 
     private void maybeShowHamburgerFeatureHighlight() {
@@ -768,6 +771,7 @@ public class DefaultContactBrowseListFragment extends ContactBrowseListFragment
 
         mWritableAccountsFuture = AccountTypeManager.getInstance(getContext()).filterAccountsAsync(
                 AccountTypeManager.writableFilter());
+        maybeRestoreSelection();
     }
 
     private void maybeHideCheckBoxes() {
@@ -776,6 +780,26 @@ public class DefaultContactBrowseListFragment extends ContactBrowseListFragment
         }
     }
 
+    private void maybeRestoreSelection() {
+        if (!mRestoreSelectionOnResume || isSearchMode()) {
+            return;
+        }
+        final ListView listView = getListView();
+        if (listView == null) {
+            return;
+        }
+        final ContactListAdapter adapter = getAdapter();
+        if (adapter == null) {
+            return;
+        }
+        final int selectedPosition = adapter.getSelectedContactPosition();
+        if (selectedPosition == -1) {
+            return;
+        }
+        mRestoreSelectionOnResume = false;
+        listView.setSelection(selectedPosition + listView.getHeaderViewsCount());
+    }
+
     public ActionBarAdapter getActionBarAdapter(){
         return mActionBarAdapter;
     }
@@ -916,6 +940,7 @@ public class DefaultContactBrowseListFragment extends ContactBrowseListFragment
         @Override
         public void onViewContactAction(int position, Uri contactLookupUri,
                 boolean isEnterpriseContact) {
+            mRestoreSelectionOnResume = true;
             if (isEnterpriseContact) {
                 // No implicit intent as user may have a different contacts app in work profile.
                 ContactsContract.QuickContact.showQuickContact(getContext(), new Rect(),
@@ -1030,6 +1055,10 @@ public class DefaultContactBrowseListFragment extends ContactBrowseListFragment
                         R.color.actionbar_icon_color), PorterDuff.Mode.SRC_ATOP);
             }
         }
+
+        if (mActivity != null) {
+            mActivity.updateDpadNavigation();
+        }
     }
 
     private void makeMenuItemVisible(Menu menu, int itemId, boolean visible) {
-- 
2.34.1

